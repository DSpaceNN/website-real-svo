import { CDFlashingDirectiveDirective } from './cdflashing-directive.directive';

describe('CDFlashingDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CDFlashingDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
